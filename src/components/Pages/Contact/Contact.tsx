import type { PageProps } from "../../..";

export default function Contact({
  setTitle,
}: {
  setTitle: PageProps;
}): React.JSX.Element {
  setTitle("Contact");

  return <>EMAIL CONTACT THINGIE</>;
}
